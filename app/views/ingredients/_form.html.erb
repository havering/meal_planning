<%= form_for(ingredient) do |f| %>
  <% if ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
      <% ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :preparation %>
    <%= f.text_field :preparation %>
  </div>

  <div class="field">
    <%= f.label :num_quant %>
    <%= f.number_field :num_quant %>
  </div>

  <div class="field">
    <%= f.label :name_quant %>
    <%= f.text_field :name_quant %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
